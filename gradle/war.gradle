apply plugin: "war"

bootWar {
    mainClassName = "tech.przybysz.pms.ProductsServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
