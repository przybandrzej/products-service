version: '2'
services:
  productsservice-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/ProductsService/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=ProductsService
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
